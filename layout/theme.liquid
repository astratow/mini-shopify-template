<!doctype html>
<html lang="{{ request.locale.iso_code }}" >

  <head>
    {{ content_for_header }}
    {{ 'base.css' | asset_url | stylesheet_tag }}
  </head>
  <body>
    {% section 'header' %}
  <main>
    <p>content</p>
    {{ content_for_layout }}
  </main>
  {% section 'footer' %}

  {{ 'theme.js' | asset_url | script_tag }}

  </body>

</html>
